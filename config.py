vudb_test = dict(host=r"host", database="database", username="user", password="password")
vudb_prod = dict(host=r"host", database="database", username="user", password="password")
log_path = r"log_path"
pushover = dict(user_key='user_key', api_token='api_token')
